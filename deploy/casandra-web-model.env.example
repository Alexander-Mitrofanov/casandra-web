# Optional operator-owned selection, installed as /etc/casandra-web-model.env.
# Keep bundles outside /srv/casandra/jobs and readable but not writable by casandrasvc.
CASANDRA_WEB_MODEL_REGISTRY=/srv/casandra/models/registry.json
# Leave default until the separately registered bundle is deliberately activated.
CASANDRA_WEB_MODEL_NAME=default
